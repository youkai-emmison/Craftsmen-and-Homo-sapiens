// Script purpose: Defines the minimal contract for objects that can receive demo melee damage.
// Key Inspector variables:
// - No Inspector variables.
public interface IDamageable
{
    void TakeDamage(int damageAmount);
}
