// Script purpose: Lists the minimal states used by BasicEnemyController.
// Key variables:
// - Idle, Patrol, Chase, Attack, Dead: The only enemy states supported in this prototype.
public enum EnemyState
{
    Idle,
    Patrol,
    Chase,
    Attack,
    Dead
}
